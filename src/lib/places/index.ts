/**
 * Places domain — types, curated catalog, and GeoJSON helpers.
 * Import from `$lib/places` in new code. `$lib/city/places` re-exports for compatibility.
 */
export * from './types';
export * from './catalog';
export * from './geo';
